{
  "name": "treeman/e2e-laravel",
  "description": "Minimal Laravel-shaped project for treeman e2e tests. Only depends on illuminate/database so composer install is fast.",
  "require": {
    "illuminate/database": "^11.0",
    "illuminate/events": "^11.0",
    "vlucas/phpdotenv": "^5.6"
  },
  "config": {
    "platform-check": false
  }
}
