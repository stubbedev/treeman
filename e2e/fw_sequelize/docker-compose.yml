services:
  postgres:
    image: postgres:16-alpine
    container_name: treeman-e2e-fw-seq-pg
    environment: { POSTGRES_PASSWORD: pgpw }
    ports: ["15512:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 3s
      retries: 30
      start_period: 5s

  node:
    image: node:20
    container_name: treeman-e2e-fw-seq-node
    command: ["sleep", "infinity"]
    volumes:
      - /tmp:/tmp
    depends_on:
      postgres:
        condition: service_healthy
