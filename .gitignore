/target
Cargo.lock.bak
*.swp
.direnv/
.envrc.local

# Go build output
/bin/

# Per-user cargo override (linker swap, sccache, etc.) — see
# .cargo/config.toml for opt-in instructions.
/.cargo/config.local.toml
